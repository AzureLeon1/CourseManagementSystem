import api from '@/api/index.js'

const state = {
    // examHasDone: false,
    // examEndTime: ""
}

const getters = {
    
}

const actions = {
    // async getPersonInfo({commit,state},id){
    //     const data = await api.getPersonInfo(id);
    //     commit('setPersonInfo',data)
    // }
}

const mutations = {
    // setExamHasDone(state,props) {
    //     state.examHasDone = props
    // },
    // setExamEndTime(state,props){
    //     state.examEndTime = props
    // }
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
